$('input:submit.primer_set_<%=@primer_set_subscription.primer_set_id %>')
    .val("Unsubscribe")
    .parent('form').attr('action', '<%= primer_set_subscription_path( @primer_set_subscription.id ) %>')
    .prepend('<input type="hidden" name="_method" value="delete">');
